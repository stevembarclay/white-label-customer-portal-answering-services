import type { MessagePriority } from '@/types/answeringService'

// Stub — replaced in Task 4 with keyword-based fallback logic.
export function assignPriority(_callType: string): MessagePriority {
  return 'low'
}
